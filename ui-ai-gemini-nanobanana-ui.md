# 博饼小游戏 UI 重设计：AI 设计图驱动工作流评估

## Executive Summary

使用 Gemini Nano Banana (Pro/2) 生成 UI 设计图再据此修改项目 UI 的方案**总体合理且推荐**，但需要理解其边界：AI 设计工具擅长快速产出视觉概念和布局方案，但不能直接生成可用代码，需要人工「翻译」为 CSS/组件修改。该项目的 UI 层结构清晰（React 纯 DOM overlay + CSS Variables + 无 CSS Modules），改造成本低、风险可控。关键在于设计好 prompt、明确约束边界、建立 Design Token 映射流程。

---

## 一、可行性评估

### 1.1 方案合理性：✅ 合理

| 维度 | 评估 | 说明 |
|------|------|------|
| **工具成熟度** | ✅ 高 | Nano Banana Pro/2 在 UI mockup 场景有大量实践案例，支持中文文本渲染、多屏一致性、sketch-to-mockup[^1] |
| **项目 UI 复杂度** | ✅ 低 | 仅 11 个 React 组件，全部是纯 DOM overlay[^2]，无复杂交互动画 |
| **样式架构** | ✅ 友好 | 全局 CSS + CSS Variables，集中 3 个样式文件[^3]，改样式不需要跨模块改组件 |
| **3D 与 UI 解耦** | ✅ 完全隔离 | 3D 场景由 Three.js 命令式管理，UI overlay 通过 `pointer-events: none` 浮于 canvas 之上[^4]，设计图只需关注 2D 面板部分 |
| **风险** | ⚠️ 中低 | AI 生成的设计图是位图/概念图，不是 Figma 文件，无法自动提取 token；需要人工对照还原 |

### 1.2 为什么合理

1. **当前 UI 是 "程序员式 UI"**：功能完备但视觉表现平淡——暗色半透明面板 + 基础排版，与"中秋节庆氛围"目标差距较大[^5]
2. **AI 设计图解决的是 "视觉方向" 问题**：不需要像素级还原，需要的是配色、布局、元素风格的方向指引
3. **改造影响面可控**：UI 修改只涉及 `src/ui/` 下的组件和 `src/ui/styles/` 下的 3 个 CSS 文件，不触及物理引擎、规则判定等 P0 核心逻辑[^6]

### 1.3 局限性

| 局限 | 影响 | 应对 |
|------|------|------|
| AI 生成的是**位图**，不是设计稿 | 无法自动提取颜色值、间距、字号 | 人工取色 + 手动标注 |
| 不了解项目约束 | 可能设计出超出实现范围的效果 | prompt 中明确约束 |
| 3D 部分无法覆盖 | 碗、骰子、桌面的材质/灯光不在 2D mockup 范围内 | 3D 场景单独调整（P2 阶段任务[^7]） |
| 非交互原型 | 无法展示 hover/active/disabled 状态 | 需要额外 prompt 或手动补充 |

---

## 二、推荐工作流

```
┌──────────────────────────────────────────────────────────┐
│                    Phase 1: 需求准备                      │
│  梳理当前 UI 组件清单 + 不满意的点 + 视觉目标关键词        │
└────────────────────────┬─────────────────────────────────┘
                         ▼
┌──────────────────────────────────────────────────────────┐
│                Phase 2: Prompt 工程                       │
│  编写结构化 prompt → 多轮对话生成 → 获得 2-3 套方案       │
└────────────────────────┬─────────────────────────────────┘
                         ▼
┌──────────────────────────────────────────────────────────┐
│              Phase 3: 设计评审与选型                       │
│  对照项目约束评估可行性 → 选定方案 → 标注 Design Tokens    │
└────────────────────────┬─────────────────────────────────┘
                         ▼
┌──────────────────────────────────────────────────────────┐
│               Phase 4: Token 映射                        │
│  将设计图中的颜色/尺寸/圆角映射到 variables.css           │
│  编写 UI Spec 清单（组件级改动对照）                      │
└────────────────────────┬─────────────────────────────────┘
                         ▼
┌──────────────────────────────────────────────────────────┐
│               Phase 5: 实施与验证                        │
│  逐组件修改 CSS + JSX → 桌面/移动端验证 → 视觉回归       │
└──────────────────────────────────────────────────────────┘
```

### Phase 1：需求准备（你来做）

**目的**：明确"不满意什么"和"想要什么"

在跟 AI 对话之前，先整理好以下信息：

#### 1a. 梳理当前 UI 组件清单

当前项目共 11 个 UI 组件，按区域分布如下[^2]：

| 区域 | 组件 | 当前表现 |
|------|------|----------|
| **顶部栏** | `RoundDisplay` | 深色半透明背景 + 白色文字，显示"第 N 轮" |
| | `SoundToggle` | 40px 图标按钮，emoji 🔊/🔇 |
| | `ResetButton` | 40px 图标按钮，文字 ↺ |
| **右侧面板** | `PrizeRecord` | 半透明卡片，奖级统计列表 |
| | `History` | 半透明卡片，最近 5 轮历史 |
| **底部区域** | `ThrowButton` | 红色大按钮，"掷骰" / "骰子翻滚中" |
| | `ResultPanel` | 半透明卡片，点数 + 奖级 + 带数 |
| | `TiltWarning` | 橙色边框面板，倾斜确认 |
| **容器** | `GameViewport` | 3D canvas + overlay 容器 |

#### 1b. 列出不满意的具体点

例如（建议但不限于）：
- 整体过于"程序员风格"，缺少节庆氛围
- 结果面板不够醒目，中奖没有惊喜感
- 侧边面板在移动端体验不好
- 按钮风格单调
- 配色过于暗沉，不够喜庆
- 字体没有传统氛围感
- ...

#### 1c. 收集视觉参考关键词

建议关键词方向：
- 中秋节 / Mid-Autumn Festival
- 闽南传统 / Minnan traditional
- 红金配色 / red and gold
- 灯笼、月饼元素
- 传统中式游戏界面
- 温暖、喜庆但不俗气

### Phase 2：Prompt 工程（你 + Gemini/Nano Banana）

**目的**：用结构化 prompt 生成高质量 UI 设计图

#### 2a. Prompt 模板（推荐使用中文或中英混合）

```text
## 设计需求

设计一个中秋博饼（闽南传统骰子游戏）Web 小游戏的 UI 界面。

### 游戏说明
- 玩家点击按钮掷 6 颗骰子到大碗中
- 根据点数组合判定奖级（从"状元插金花"到"一秀"共 13 级）
- UI 浮于 3D 场景（俯视圆桌+大瓷碗+骰子）之上

### 界面要素（必须包含）
1. **顶部栏**：左侧显示"第 N 轮"，右侧有音效开关和重置按钮（图标形式）
2. **右侧面板**（桌面端）：奖级统计卡片 + 最近 5 轮历史记录卡片
3. **底部居中**：大尺寸掷骰按钮（文字"掷骰"）
4. **底部居中**：结果面板——大号奖级名称 + 6 个骰子点数展示（"四"用红色）+ 带数显示

### 视觉风格
- 中秋节庆氛围：红色、金色、米白为主色调
- 参考传统中式节庆风格，温暖喜庆但不俗气
- 面板使用半透明毛玻璃效果或传统纹样边框
- 按钮要有质感，大尺寸适合触摸操作
- 整体风格克制典雅，不要过度装饰

### 技术约束
- 仅设计 2D overlay UI 部分，3D 场景（桌面/碗/骰子）作为背景
- 需要考虑桌面端（宽屏）和移动端（竖屏）两套布局
- 背景暗色调（深棕/深暖色），UI 元素需在暗背景上清晰可读

### 输出要求
- 请生成一张高分辨率的 UI 设计图
- 桌面端版本：canvas 占满视口，UI 面板浮于其上
- 包含明确的配色方案（给出 HEX 色值）
```

#### 2b. 多轮迭代建议

| 轮次 | 目标 | Prompt 方向 |
|------|------|-------------|
| 第 1 轮 | 获取整体布局方向 | 用上方模板生成桌面端全景 |
| 第 2 轮 | 移动端布局 | "基于上方设计，出一版移动端竖屏布局" |
| 第 3 轮 | 结果面板特写 | "放大结果面板区域，展示中奖（状元）和未中奖两种状态" |
| 第 4 轮 | 按钮状态 | "展示掷骰按钮的 3 种状态：默认、翻滚中（禁用）、hover" |
| 第 5 轮 | 配色方案提取 | "请列出这套设计的完整配色表，包含 HEX 值和用途说明" |

#### 2c. 重要提示

- **每次只要求一个焦点**，不要一次让 AI 出太多内容
- **明确给出约束**：例如"不要使用渐变色"、"文字必须清晰可读"
- **要求输出色值**：AI 图像模型生成的颜色可能不准确，要求以文字形式同时输出色值
- **保存所有版本**：建议保存到 `docs/design/` 目录下，方便后续对照

### Phase 3：设计评审与选型

**目的**：对照项目实际约束评估设计图可行性

#### 3a. 评审清单

对每个设计方案逐项评估：

- [ ] 所有 11 个 UI 组件是否都覆盖到？
- [ ] 暗背景上的文字对比度是否足够？（WCAG AA 标准：≥ 4.5:1）
- [ ] 触摸目标是否 ≥ 44px？（项目约束 `--touch-min: 44px`[^8]）
- [ ] 桌面端右侧面板是否遮挡 3D 场景关键区域（碗）？
- [ ] 移动端布局是否合理？（canvas 上半 + UI 下半[^9]）
- [ ] 动效是否可用纯 CSS 实现？（项目不使用 framer-motion 等动画库）
- [ ] 是否需要额外字体资源？（当前使用系统字体栈[^10]）
- [ ] 是否需要额外图片/图标资源？（当前 `public/` 和 `src/assets/` 均为空[^11]）

#### 3b. 选型决策

建议生成 2-3 套方案，根据以下维度打分选型：

| 维度 | 权重 | 说明 |
|------|------|------|
| 氛围感 | 30% | 是否营造出中秋节庆感觉 |
| 信息清晰度 | 30% | 奖级、点数、按钮状态是否一目了然 |
| 实现成本 | 25% | 纯 CSS 能否还原？是否需要额外资源？ |
| 移动端友好 | 15% | 竖屏布局是否合理 |

### Phase 4：Token 映射（最关键的桥梁步骤）

**目的**：将设计图"翻译"为代码可执行的规格说明

#### 4a. 更新 CSS Variables

当前 `variables.css` 的完整定义[^5]：

```css
:root {
  --color-primary: #c0392b;        /* 节庆红 */
  --color-primary-hover: #a93226;
  --color-primary-disabled: #b07070;
  --color-gold: #d4a017;           /* 金色 */
  --color-gold-light: #f0d060;
  --color-bg: #1c1410;             /* 深暖棕背景 */
  --color-panel: rgba(30, 20, 15, 0.85);
  --color-panel-border: rgba(212, 160, 23, 0.35);
  --color-text: #f5f0e8;
  --color-text-muted: #b0a89a;
  --color-text-highlight: #f0d060;
  --radius: 8px;
  --radius-lg: 12px;
  --touch-min: 44px;
}
```

根据选定的设计图，你需要输出一个新的 `variables.css`，例如：

```css
:root {
  /* 来自设计图方案 A */
  --color-primary: #d4423a;        /* 设计图主红色 */
  --color-primary-hover: #bf3830;
  --color-primary-disabled: #c08080;
  --color-gold: #daa520;           /* 设计图金色 */
  /* ... 其他 token */
}
```

#### 4b. 编写 UI Spec 清单

对每个需要改动的组件，编写明确的改动规格：

```markdown
## ResultPanel 改动规格

- 背景：从 `rgba(30,20,15,0.85)` 改为 `rgba(XX,XX,XX,0.9)` + backdrop-filter: blur(8px)
- 边框：从 `1px solid rgba(212,160,23,0.35)` 改为 `2px solid #XXX` + 圆角 16px
- 奖级文字：字号从 1.5rem 改为 2rem，增加 text-shadow
- 骰子点数：从纯色方块改为带阴影的圆形
- 动画：结果出现时增加 scale bounce 动画
```

这份清单就是后续让 Copilot 或你自己改代码的直接输入。

### Phase 5：实施与验证

**目的**：逐步改造代码，确保不破坏功能

#### 5a. 实施顺序（推荐）

```
1. variables.css              ← 先改 token，全局生效
2. global.css                 ← 字体、基础样式
3. game.css 布局部分           ← 调整面板位置、尺寸
4. game.css 组件样式           ← 逐个组件改造
5. JSX 结构调整（如需要）      ← 添加新 DOM 元素（装饰、图标）
6. 新增资源文件（如需要）      ← 字体、图标、背景纹理
```

#### 5b. 验证清单

改造完成后逐项验证：

- [ ] `pnpm build` 通过
- [ ] `pnpm test` 全部通过（UI 改动不应影响逻辑测试）
- [ ] 桌面端布局正确
- [ ] 移动端（≤768px）布局正确
- [ ] 小屏（≤480px）布局正确
- [ ] 按钮 hover/active/disabled 状态正常
- [ ] 结果面板：中奖和未中奖两种状态正确显示
- [ ] 倾斜确认面板正常工作
- [ ] 文字在暗背景上可读
- [ ] 触摸目标 ≥ 44px

---

## 三、当前 UI 架构分析（供 Prompt 参考）

### 3.1 整体布局结构[^4]

```
┌─────────────────────────────────────────┐
│  GameViewport (relative)                │
│  ┌───────────────────────────────────┐  │
│  │  canvas-host (3D scene)           │  │
│  │  ┌─────────────────────────────┐  │  │
│  │  │  game-overlay (absolute)    │  │  │
│  │  │  ┌────────┐   ┌──────────┐ │  │  │
│  │  │  │top-bar │   │  [🔊][↺]│ │  │  │
│  │  │  └────────┘   └──────────┘ │  │  │
│  │  │              ┌────────────┐ │  │  │
│  │  │              │ side-panel │ │  │  │
│  │  │              │ (right)    │ │  │  │
│  │  │              └────────────┘ │  │  │
│  │  │  ┌──────────────────────┐   │  │  │
│  │  │  │  bottom-area         │   │  │  │
│  │  │  │  [ResultPanel]       │   │  │  │
│  │  │  │  [ThrowButton]       │   │  │  │
│  │  │  └──────────────────────┘   │  │  │
│  │  └─────────────────────────────┘  │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

### 3.2 样式文件结构[^3]

| 文件 | 行数 | 职责 |
|------|------|------|
| `variables.css` | 25行 | CSS Variables（颜色、尺寸 token） |
| `global.css` | 22行 | Reset + 字体 + body 基础样式 |
| `game.css` | 371行 | 所有组件样式 + 动画 + 响应式 |

### 3.3 改造友好点

1. **所有颜色值都通过 CSS Variables 引用**[^5]——改 `variables.css` 可全局换肤
2. **没有使用 CSS Modules**——组件和样式之间是松耦合的全局 class
3. **响应式断点清晰**[^9]：768px（平板）、480px（手机）
4. **React 组件极简**[^2]——大多数只有 10-30 行，没有复杂嵌套

### 3.4 潜在改造难点

1. **单文件 CSS**：`game.css` 有 371 行[^12]，所有组件样式在一个文件里，大改动时需小心命名冲突
2. **无设计系统**：没有 Tailwind/组件库，所有样式手写，但这也意味着改动自由度最大
3. **emoji 图标**：SoundToggle (🔊/🔇) 和 ResetButton (↺) 使用 emoji/字符[^13]，可能需要替换为 SVG 图标

---

## 四、项目特殊考量

### 4.1 不要改 3D 场景部分

设计图只应关注 2D overlay UI。3D 部分（桌面、碗、骰子、灯光、装饰物）由独立的 `scene/` 模块管理[^14]，属于 P2 氛围表现任务[^7]，与 UI 改造解耦。

### 4.2 保持架构约束

UI 改造时必须遵守的架构约束[^6]：
- **UI 组件只通过 selector 读取 store**，不直接调用 store 的 set 方法
- **所有业务操作通过 `useGameController()` 获取的 controller 实例**
- **响应式归 CSS**，React 层不处理布局逻辑

### 4.3 Prompt 中应包含的背景 3D 信息

生成设计图时，需要让 AI 理解 3D 背景的大致样子（否则设计的 UI 可能不协调）：

> 背景是一个俯视角度的 3D 场景：深棕色圆形木桌，中央放一个白色大瓷碗，碗中有 6 颗象牙白骰子（红色四点）。桌上还有几个月饼和两个红色小灯笼作为装饰。整体色调暖棕，场景灯光偏暖。

---

## 五、替代方案评估

| 方案 | 优点 | 缺点 | 推荐度 |
|------|------|------|--------|
| **A: Gemini Nano Banana 生成设计图 → 手动还原** | 快速获得视觉方向；成本低；灵活度高 | 需要人工"翻译"；可能需多轮迭代 | ⭐⭐⭐⭐⭐ **推荐** |
| B: Figma 手动设计 → 导出 CSS | 精确控制；可导出 token | 需要设计技能；耗时长 | ⭐⭐⭐ |
| C: 直接用 CSS 框架（如 Tailwind） | 快速应用预设主题 | 需要引入新依赖；迁移工作量大 | ⭐⭐ |
| D: 使用现有中式 UI 组件库 | 自带节庆风格 | React 生态中缺少合适的库；可能与项目架构冲突 | ⭐ |
| E: V0/Bolt 类 AI 直接生成代码 | 端到端自动化 | 生成的代码质量参差不齐；难以与现有架构集成 | ⭐⭐ |

---

## 六、实用 Tips

### 6.1 取色技巧

AI 生成的位图中颜色可能因压缩/渲染不准确：
- 使用 macOS 自带的"数码测色计"（Digital Color Meter）取色
- 或在 Figma 中导入图片后使用取色器
- 关键颜色建议在 prompt 中要求 AI **以文字形式列出 HEX 值**

### 6.2 字体选择

如果设计图中使用了书法/手写风格字体，推荐的 Web 字体：
- **ZCOOL XiaoWei** (站酷小薇)：免费，适合标题
- **Ma Shan Zheng** (马善政毛笔楷书)：免费，适合装饰性文字
- 来源：Google Fonts 免费提供

### 6.3 保存设计产出

建议在项目中创建目录结构：

```
docs/
├── design/
│   ├── v1-desktop.png    ← AI 生成的桌面端设计图
│   ├── v1-mobile.png     ← AI 生成的移动端设计图
│   ├── v1-result-panel.png ← 结果面板特写
│   ├── color-palette.md  ← 配色方案文字记录
│   └── ui-spec.md        ← 组件级改动规格说明
```

### 6.4 给 Copilot 的改造 Prompt 模板

拿到设计图和 UI Spec 后，用类似以下 prompt 让 Copilot 执行改造：

> 参考 `docs/design/v1-desktop.png` 的设计方案，按照 `docs/design/ui-spec.md` 中的改动规格，修改 `src/ui/styles/variables.css` 和 `src/ui/styles/game.css`。
> 
> 要求：
> 1. 先改 variables.css 的 token 值
> 2. 再改 game.css 的组件样式
> 3. 保持响应式断点不变（768px / 480px）
> 4. 不改 React 组件逻辑
> 5. 改完后运行 `pnpm build` 确认编译通过

---

## Confidence Assessment

| 判断 | 置信度 | 说明 |
|------|--------|------|
| 方案总体可行 | ✅ 高 | 基于项目架构实际分析 |
| Nano Banana 能生成高质量中式 UI mockup | ✅ 高 | 有公开实践案例 |
| CSS 改造不会影响核心逻辑 | ✅ 高 | UI 层与物理/规则层完全解耦 |
| 改造工作量可控（1-2 天） | ⚠️ 中 | 取决于设计方案复杂度 |
| 多轮 Prompt 迭代能获得满意结果 | ⚠️ 中 | AI 图像生成有随机性，需要耐心调整 |

---

## Footnotes

[^1]: Nano Banana Pro 支持 UI mockup 生成，参考 https://uxplanet.org/ui-design-with-nano-banana-pro-51aa803457d5 和 https://dev.to/oikon/nano-banana-special-prompt-achieved-rapid-mobile-ui-mockups-1mif
[^2]: `src/ui/components/` 目录下 11 个组件文件：GameViewport.tsx、ThrowButton.tsx、ResultPanel.tsx 等
[^3]: `src/ui/styles/` 目录下 3 个文件：variables.css (25行)、global.css (22行)、game.css (371行)
[^4]: `src/ui/styles/game.css:17-28` — `.game-overlay` 使用 `position: absolute; inset: 0; pointer-events: none` 浮于 canvas 之上
[^5]: `src/ui/styles/variables.css:1-25` — 完整 CSS Variables 定义
[^6]: `ARCHITECTURE.md:18-26` — 架构原则第 1、2、6 条约束了 UI 层行为
[^7]: `CHECKLIST.md:197-234` — Phase 3 (P2) 氛围表现任务
[^8]: `src/ui/styles/variables.css:24` — `--touch-min: 44px`
[^9]: `src/ui/styles/game.css:319-361` — 移动端响应式断点 768px
[^10]: `src/ui/styles/global.css:10-11` — 系统字体栈：-apple-system, PingFang SC 等
[^11]: `public/` 和 `src/assets/` 目录均为空
[^12]: `src/ui/styles/game.css` — 完整样式文件 371 行
[^13]: `src/ui/components/SoundToggle.tsx:17` — 使用 emoji 🔊/🔇 和 `src/ui/components/ResetButton.tsx:14` — 使用字符 ↺
[^14]: `apps/web/src/scene/` 当前包含 setup.ts、table.ts、bowl.ts；旧 decorations 实验未接入运行时，已删除。

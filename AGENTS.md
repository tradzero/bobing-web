# 项目介绍

用 Three.js + cannon-es 编写一个基于闽南传统中秋博饼的 Web 小游戏，优先保证项目结构清晰、运行稳定、结果判定可靠，其次再追求视觉表现。

> **文档索引**
>
> | 文档 | 内容 |
> | ---- | ---- |
> | [ARCHITECTURE.md](./ARCHITECTURE.md) | 技术栈、架构原则、项目结构、数据流、状态机、Store 设计、奖级优先级表与带数规则、碗碰撞体方案、测试策略、实现优先级 |
> | [CHECKLIST.md](./CHECKLIST.md) | 分阶段开发任务清单（Phase 0 初始化 → Phase 1 核心引擎 → Phase 2 UI → Phase 3 氛围表现），含 \[实现\]/\[测试\]/\[验收\] 标签 |
> | [UI-CHECKLIST.md](./UI-CHECKLIST.md) | UI 收敛清单、移动端布局不变量、后续素材待办与海碗纹样交付规范 |

---

## 整体风格

- 中秋节氛围场景，暖色调主光源 + 柔和环境光
- 当前保留传统圆桌占位，中央放置大瓷碗（海碗），碗口朝上；后续优先考虑桌布方案，而不是重做整桌
- 当前阶段不再推进月饼、灯笼等独立 3D 摆件，时间预算优先留给海碗、桌布和 UI 收口
- 摄像机俯视 + 轻微倾斜，始终聚焦碗与桌面中心，角度不可调节

## 核心玩法

- 6 颗标准骰子（1-6 点），点数"四"使用红色特殊标记
- 玩家点击"掷骰"，骰子从碗上方随机投入碗中，经碰撞翻滚后停稳
- 读取每颗骰子朝上点数，根据博饼规则自动判定奖级
- 命中多个奖级时只取最高优先级一项；未命中显示"未中奖"
- 结果同时展示 6 颗骰子点数组合与奖级，便于核对
- 骰子材质为骨质/象牙白，"四点"使用红色凸点或红漆效果

## 博饼规则要求

- 奖级规则为清晰、可枚举、可维护的数据结构，不硬编码条件分支
- 明确状元子级判定规则与优先级（五红、五子登科、满堂红、遍地锦等）
- 完整奖级优先级表（13 级）与带数规则 → 见 [ARCHITECTURE.md § 奖级优先级表](./ARCHITECTURE.md)

## 关键实现约束

以下为核心设计约束，具体方案与参数结构见 [ARCHITECTURE.md](./ARCHITECTURE.md)。

**物理模拟**（→ 架构文档 § 碗碰撞体方案、config/physics.ts、config/throw.ts）

- 固定时间步长，渲染循环与物理解耦
- 碗碰撞体用 Heightfield 连续碗底 + 竖直挡墙，不依赖 Trimesh
- 骰子与碗、骰子之间使用可独立调参的接触材质
- 投掷加入受控随机性，避免极端初始参数导致穿透或飞出
- 开启 sleep 机制辅助停稳检测
- 所有物理参数不预设固定值，以实际调试表现为准

**停稳判定与点数读取**（→ 架构文档 § dice/settle.ts、dice/read-face.ts、config/settle.ts）

- 明确停稳判定：全部 sleep 或连续一段时间速度/角速度低于阈值
- 超时兜底：进入超时结算路径，避免流程卡死
- 点数读取基于六面法线与世界 up 向量点积，不依赖欧拉角区间

**当前视觉占位策略**（→ 架构文档 § 当前视觉占位策略、UI-CHECKLIST.md § 海碗素材规范）

- 海碗当前允许使用程序化青花纹样占位，最终再替换正式纹样素材
- 桌面当前允许保留程序化木纹与桌面圈层，占位即可；正式美术方向优先桌布，不再继续扩展桌面摆件
- 不要再为未接入运行时的桌面装饰物单独投入实现时间，除非用户明确要求恢复该路线

## UI 要求

- 核心 UI：掷骰按钮、当前轮次、结果面板、累计奖级记录、重置按钮、音效开关
- 掷骰中按钮禁用并显示"骰子翻滚中"
- 结果面板采用传统节庆风格（红、金、米白），突出奖级与点数
- 最近 5 轮历史记录
- 移动端采用真实上下布局，不再使用可拖拽底部浮层
- 兼顾桌面端和移动端

## 音效与反馈

- 可选轻量音效（碰撞声、滚动声、中奖提示音），支持静音切换
- 高频碰撞通过节流或强度筛选避免叠音
- 中奖视觉反馈风格克制

## 编码要求

- 关键逻辑附带中文注释（点数读取、奖级判定、碗碰撞体、停稳检测）

## 验收标准

- 首屏稳定展示桌面、海碗与 6 颗骰子
- 连续多轮不出现穿模、永久抖动、不结算、按钮状态异常
- 每轮可靠输出朝上点数并正确映射奖级
- 完整流程：掷骰 → 翻滚 → 停稳 → 结算 → 展示 → 下一轮
- 桌面端与移动端均可操作

## 实现优先级

1. **P0**：物理稳定、点数读取准确、奖级判定正确、流程不卡死
2. **P1**：UI 清晰、操作反馈明确、移动端可用
3. **P2**：中秋氛围装饰、音效、粒子和高级表现

---

## 工作流

当用户反馈审查结果之后, *必须* 不能马上修改代码, 按照: 思考问题是否存在(独立测试核查, 不要全盘接受) -> 核实问题 -> 提出方案/解决办法(当不确定或者需要用户确认时) 将问题列出清单让用户确认, 决策 -> 用户最终确认后再修改代码.

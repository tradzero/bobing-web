# UI-CHECKLIST

## Phase 1 2D Overlay First Pass

- [x] 建立 2D UI 设计 token（字体、金红配色、毛玻璃面板、阴影层级）
- [x] 顶部半透明栏改造（轮次显示、音效/重置按钮统一到同一视觉系统）
- [x] 结果 popup 改成节庆化面板骨架（大标题、骰子展示、金色边框、云纹/灯笼装饰）
- [x] 奖级统计卡片改造为节庆化右侧面板
- [x] 最近 5 轮历史记录改造为节庆化右侧面板
- [x] 主按钮改造成金色节庆按钮
- [x] 倾斜提示面板风格与结果 popup 对齐

## Phase 2 2D Overlay Polish

- [x] 第一轮桌面/移动间距与层级收口：桌面侧栏收窄并上移，短视口结果卡压缩；移动顶栏改为 `82px + safe-area` 单行，6 颗结果骰保持单行
- [x] 结果 popup 动效和显隐节奏优化
- [x] 右侧面板装饰细节补齐（标题条、角饰、分隔线）
- [x] 顶栏图标精修为更接近设计图的视觉效果
- [x] 掷骰按钮 hover / disabled / rolling 状态精修
- [x] rolling 合成成本收口：顶栏关闭大面积背景 blur，移除逐帧 shadow pulse，保留 transform/opacity 角饰动效
- [x] ResultPanel 的系统“云/兔”字符替换为纯 CSS 细线角纹，避免平台字形差异且不增加图片、纹理或字体资源
- [x] 移动端交互优先级固化：投掷按钮 > 结果/倾斜确认 > 历史/统计
- [x] 移动端上下布局：结算卡可轻侵入游戏区边界，但奖级统计/历史记录回归真实内容区并随页面整体滚动

## Mobile Layout Invariants

- [x] 移动端唯一主操作按钮始终是掷骰按钮，且位于结算卡之后
- [x] 当轮结果与倾斜确认属于当前回合强反馈，必须排在历史数据之前
- [x] 历史记录与累计统计属于回看信息，位于按钮之后并随页面整体滚动，不再使用独立浮板或内部滚动区抢占视口
- [x] 结算卡属于底部内容区顶部，可轻侵入游戏区边界营造悬浮感，但不能脱离文档流覆盖整个游戏画面
- [x] 初始态内容区默认收起为提示边，仅提示用户下方存在记录区；首轮结算后再展开真实内容区

## Phase 3 Deferred Scene Work

- [x] 海碗外壁程序化青花第一轮收口：既有单张 CanvasTexture 使用无缝云头/细折枝带，降低大团花抢焦与瓷釉硬热点
- [x] 海碗青花资产接入：单张 304KB 横向无缝 WebP 直接作为 wall texture，半幅重排与镜像拼接消除 UV 首尾断口；首帧纹样渲染前显示不透明加载页，失败或 8 秒超时替换成程序化 CanvasTexture；桌面/移动 WebGL 截图与 2 mesh / 2 material / 1 活动 texture 预算通过
- [x] 桌面程序化木纹第一轮收口：既有单张 CanvasTexture 改为细密长向纹、少量淡年轮，并降低嵌饰圈透明度
- [ ] 在保留现有桌体的前提下设计桌布方案，并校准桌布与碗区构图关系
- [ ] 桌布边缘、包边、压纹或垂边细节补齐
- [x] 当前保持既有俯视加轻微倾斜镜头；镜头前推不再作为默认待办，未来改视角需用户明确同意并重做遮挡验收

## 建议补充素材

- [x] 占位符推进策略：图标、纹样、木纹、字体可先用占位资源或程序化样式推进，统一在最终美术收口阶段替换正式素材
- [ ] 青花瓷碗纹样素材：用于海碗外壁和碗口边饰，优先级最高
- [ ] 桌布素材：用于覆盖现有桌面而不是重做桌体，建议同时准备主体花纹与边缘包边素材
- [ ] 统一风格的 SVG 图标：至少包含音效开关、重置按钮，可选补充云纹/灯笼等装饰图形
- [ ] 可商用中文展示字体：用于标题、轮次和按钮文案，提升稳定性和设计稿一致性
- [ ] 参考图补充：若有更明确的移动端首屏、结果态、桌面端侧栏细节图，可继续用于收敛视觉差异

## 海碗素材规范

- [ ] 外壁主纹样交付一份横向无缝展开图，推荐尺寸 2048x512，宽高比 4:1
- [ ] 碗口边饰单独交付一份窄条纹样，推荐尺寸 2048x256，宽高比 8:1
- [ ] 优先保留 SVG 源稿，游戏内直接使用 PNG 贴图；两种格式都需要留档
- [ ] PNG 建议导出透明底正式版，同时额外导出一份米白底预览版，便于对稿
- [ ] 纹样必须左右无缝衔接，不能带透视、投影、光照、高光烘焙
- [ ] 纹样应按正视展开图绘制，不要按碗的透视效果直接出图
- [ ] 配色建议控制在青花蓝 + 瓷白体系，避免把金边、阴影、旧化噪点直接烘焙进素材
- [ ] 主纹样建议保留安全边距，左右各至少 32px，避免贴图接缝处关键图案被切断
- [ ] 如使用 AI 生成，需额外保留可编辑母版或重绘参考图，避免后续无法局部修纹样

## Validation

- [x] 运行 build 与测试，确认 UI 重构未破坏逻辑
- [x] 桌面端视觉回归
- [x] 移动端视觉回归（用户于 2026-08-14 确认此前版本可接受；仅为人工主观验收）
- [x] schema v9 `bench:browser` 已保存并人工复核本轮最新桌面/移动 settled 截图：六骰、碗口、结果、按钮、统计与历史可核对，无横向或内部滚动
- [ ] 在不同浏览器与记录 GPU/驱动/DPR 的真实设备上复核最新画面；SwiftShader 截图和当前设备主观确认不作为跨设备结论

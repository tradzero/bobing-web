# UI 与素材清单

## 已确认的不变量

- [x] 红、金、米白节庆色；骨白骰子，四点使用红色
- [x] 桌面端顶栏、结果卡、主按钮和侧栏信息层级清晰
- [x] 移动端使用真实上下文档流，不使用可拖拽底部浮层
- [x] 当前结果/倾斜/错误优先于主按钮，历史与累计统计位于其后
- [x] rolling 禁止重复投掷并显示明确状态；异常不能伪装为中奖
- [x] 摄像机保持俯视加轻微倾斜，聚焦海碗和桌面中心
- [x] 只保留圆桌、海碗和 6 颗骰子，不主动增加月饼/灯笼等独立 3D 摆件

## 当前实现

- [x] 6 颗骰子使用单材质 atlas 和 `InstancedMesh`，资源随 `DiceSet` 显式释放
- [x] 桌面使用细密长向程序化木纹和克制嵌饰圈
- [x] 海碗使用 `bowl-blue-white-seamless-v2.webp`；旧 v1 图和根目录生成原图已删除
- [x] 碗纹通过半幅重排与镜像拼接隐藏 UV 首尾接缝
- [x] 纹样解码和首个渲染完成前使用不透明加载页；失败/8 秒超时切换程序化纹样
- [x] 两条碗纹路径都只保留一张活动 wall texture
- [x] rolling 时关闭高成本大面积背景采样和按钮 shadow pulse
- [x] reduced 质量档 rolling 主画布 DPR 限为 1x；full 档保持基础 DPR
- [x] ResultPanel 角纹为纯 CSS，不依赖平台云/兔字形

## 移动端布局门禁

- [x] 主要操作触摸目标满足现有 `--touch-min` 约束
- [x] 6 颗结果骰保持可读，不发生横向滚动
- [x] 结算卡可轻度侵入游戏区边界，但仍在文档流中
- [x] 历史/统计随页面整体滚动，不创建抢占视口的内部滚动区
- [x] 初始记录区只显示提示边；产生内容后再展开

## 后续美术

- [ ] 在保留当前桌体的前提下设计桌布主体、包边、压纹或垂边
- [ ] 补统一风格的 SVG 音效/重置图标
- [ ] 选择可商用中文展示字体并验证加载失败回退
- [ ] 收集更明确的移动首屏、结果态和桌面侧栏参考图

## 后续替换素材规范

- 碗外壁纹样优先交付横向无缝展开图，建议 2048×512、4:1；如另做口沿边饰，建议 2048×256、8:1。
- 游戏产物优先使用压缩 WebP；同时保留可编辑源稿和米白底对稿预览，不再要求运行时使用 PNG。
- 左右边缘必须无缝，不能烘焙透视、投影、灯光、高光或金边；关键图案避开接缝安全区。
- 配色保持青花蓝与瓷白，资产替换不得增加 mesh/material/texture 数量或降低骰面可核对性，除非另有测量和批准。

## 验收

- [x] 当前桌面/移动 Playwright 流程和结构性能门禁已建立
- [x] 加载、rolling、result、tilt、error、reset 和静音状态有自动化覆盖
- [x] 多浏览器加入、开局、投掷同步、轮次交接和刷新恢复的基础自动化
- [x] 开放房大厅、创建/发现入口、动态房间链接和每房间独立身份
- [x] 四浏览器同时运行两个房间的成员、投掷与 UI 广播隔离
- [x] 创建开放房时同时填写昵称并自动恢复原子绑定的房主身份
- [x] `abandoned` 明确提示长期无人操作，原房主可按锁定阵容再开一局
- [x] WebSocket 中断和服务进程重启后显示断线提示并自动恢复原身份
- [ ] 多人旁观、结束选择和加投的视觉与触屏自动化
- [ ] 在记录浏览器、GPU、DPR、设备和电源模式的真实设备上做跨环境复核
